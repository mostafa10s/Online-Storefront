/* Replace with your SQL commands uu id*/

CREATE TABLE users (
    user_id SERIAL PRIMARY KEY,
    last_name varchar(50) NOT NULL,
    first_name varchar(50) NOT NULL,
    passworde varchar(50)NOT NULL,
    user_name varchar(50)NOT NULL
);
